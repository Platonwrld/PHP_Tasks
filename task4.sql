CREATE TABLE "sportsman" ("id" integer NOT NULL PRIMARY KEY AUTOINCREMENT, "name" varchar(100) NOT NULL, "last_name" varchar(100) NOT NULL, "father_name" varchar(100) NOT NULL, "number" integer NOT NULL, "birthday" varchar(100) NOT NULL, "age" integer NOT NULL, "bio" text NOT NULL);
COMMIT;
